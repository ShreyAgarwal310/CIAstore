<!DOCTYPE HTML>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="style.css">
    <title>{code} {name}</title>
  </head>
  <body>
    <div class="content">
      <h1>{{ code }} {{ name }}</h1>
      <p>{{ desc }}</p>
      {% if code == 401 %}
      <p>Please <a href="/login">login to view this page</a></p>
      {% endif %}
    </div>
    <footer>
      <i>If you're reading this, the web server was installed correctly.™</i>
      <hr>
      <p>
        Caught In the Act Server v0.0.0 © CSHS Members
      </p>
    </footer>
  </body>
</html>