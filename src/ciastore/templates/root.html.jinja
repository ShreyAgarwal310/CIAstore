<!DOCTYPE HTML>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="style.css">
    <title>Caught In the Act</title>
  </head>
  <body>
    <div class="content">
      <h1>Caught In the Act</h1>
      <div class="box">
        {% if username == "" %}
        <p>
          Please log in at <a href="/login">this link</a>.
        </p>
        {% else %}
        <p>
          Hello logged in user {{ username }}.
        </p>
        {% endif %}
      </div>
      <p>Links:</p>
      <ul>
        {% if username == "" %}
        <li><a href="/login">Log In</a></li>
        <li><a href="/signup">Sign Up</a></li>
        {% else %}
        <li><a href="/user_data">[DEBUG] View user data</a></li>
        <li><a href="/logout">Log Out</a></li>
        <li><a href="/settings">Account Settings</a></li>
        <li><a href="/tickets">View ticket count</a></li>
        <li><a href="/add-tickets">Add Tickets for Student</a></li>
        <li><a href="/invite-teacher">Invite Teacher</a></li>
        {% endif %}
      </ul>
    </div>
    <footer>
      <i>If you're reading this, the web server was installed correctly.™</i>
      <hr>
      <p>
        Caught In the Act Server v0.0.0 © CSHS Members
      </p>
    </footer>
  </body>
</html>